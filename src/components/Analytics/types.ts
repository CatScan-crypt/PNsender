export interface AnalyticsData {
  data_field: {
    id: string;
    campaign: string;
    start: string;
    end: string;
    status: string;
    target: string;
    last_updated: string;
    sends_impressions: string;
    clicks_opens: string;
  };
} 